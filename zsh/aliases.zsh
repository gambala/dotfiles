alias reload!='. ~/.zshrc'

alias cls='clear' # Good 'ol Clear Screen command

alias cpd='cap production deploy'
alias cpdsc='cap production deploy:sitemap:create'
alias cpdsr='cap production deploy:sitemap:refresh'
alias cps='cap production setup'
alias csd='cap staging deploy'

alias hosts='sudo subl /etc/hosts'
